#!/bin/bash
awk -v a="$WATER" -v b="$STIR" 'BEGIN{w="water" s="tir." o="shtbeolhc" for(i=1;i<=length(a);i++)x=x*5+index(w,substr(a,i,1))-1 for(i=1;i<=length(b);i++)y=y*4+index(s,substr(b,i,1))-1 n=x+y while(n){r=substr(o,n%9+1,1) r;n=int(n/9)} if(r=="")r=substr(o,1,1) print r}'
